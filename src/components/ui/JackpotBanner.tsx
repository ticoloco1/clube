'use client';

/** Jackpot descontinuado na UI pública — componente mantido para não quebrar imports. */
interface JackpotBannerProps {
  compact?: boolean;
  showInSite?: boolean;
  accentColor?: string;
}

export function JackpotBanner(_props: JackpotBannerProps) {
  return null;
}
